# Copy this file to local-config.properties

# Change profile here so all tests can use same db
spring.profiles.active=h2

# Possible values: none, validate, update, create-drop
spring.jpa.hibernate.ddl-auto=none
spring.liquibase.enabled=true

### Additional logging ###

# Pretty print sql
#spring.jpa.properties.hibernate.format_sql=true

# Log sql queries via logback
#logging.level.org.hibernate.SQL=DEBUG

# Show bind values for '?' placeholders
#logging.level.org.hibernate.orm.jdbc.bind=TRACE

# Show sql comments (not really useful)
#spring.jpa.properties.hibernate.use_sql_comments=true